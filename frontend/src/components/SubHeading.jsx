import React from 'react'

const SubHeading = ({title}) => {
  return (
          <small className="text-sm sm:text-[1rem] text-center ">
              {title}
          </small>
  );
}

export default SubHeading